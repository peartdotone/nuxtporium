{
  "private": true,
  "repository": "git@github.com:peartdotone/nuxtporium.git",
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run dev --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
